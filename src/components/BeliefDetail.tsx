import React from 'react';
import { BeliefStatus, SavedBelief } from '../util';

interface Props {
  belief: SavedBelief | null;
}

export default function BeliefDetail({ belief }: Props): JSX.Element {
  return <></>;
}